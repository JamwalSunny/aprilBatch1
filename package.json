{
  "name": "testmd-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Cypress E2E tests for TestMD claims",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run --browser electron --headless",
    "test": "npm run cypress:run"
  },
  "devDependencies": {
    "cypress": "^13.15.0",
    "cypress-xpath": "^2.0.1"
  }
}